CREATE TABLE `ad_dim`.`dim_site_reldb_site_code_info_tmp`(
    `rowkey` STRING COMMENT 'byteTable rowkey',
    `id` BIGINT COMMENT 'code_id',
    `site_id` BIGINT COMMENT '站点id',
    `user_id` BIGINT COMMENT '用户ID',
    `code_name` STRING COMMENT '广告位名称',
    `height` BIGINT COMMENT 'iframe长度|广告位长度',
    `height_scale` BIGINT COMMENT '高度--比例值',
    `width` BIGINT COMMENT 'iframe宽度|广告位宽度',
    `width_scale` BIGINT COMMENT '宽度-比例值',
    `district` STRING COMMENT '黑科技地区',
    `style_js` STRING COMMENT '样式js文件',
    `dest_cpm` STRING COMMENT '目标cpm， 单位：元',
    `min_ecpm` STRING COMMENT '保底ECPM',
    `channel` STRING COMMENT '渠道',
    `app_channel` STRING COMMENT 'APP媒体的渠道: {appid: channel}',
    `download_url` STRING COMMENT '下载链接',
    `app_download_url` STRING COMMENT 'APP媒体的下载链接: {appid: url}',
    `status` BIGINT COMMENT '代码位状态, 1:等待 2:运行 3:停止',
    `extra` STRING COMMENT '代码位其他信息',
    `app_code_type` BIGINT COMMENT 'APP代码位类型 1:信息流 2:banner 3:开屏',
    `media_type` BIGINT COMMENT '媒体类型 1: wap 2:app',
    `mask_business` STRING COMMENT 'APP行业屏蔽',
    `mask_business_new` STRING COMMENT '""',
    `mask_keywords` STRING COMMENT '关键词屏蔽',
    `mask_apps` STRING COMMENT 'APP屏蔽',
    `mask_adx_ids` STRING COMMENT 'adx广告屏蔽',
    `ctime` BIGINT COMMENT '创建时间',
    `mtime` BIGINT COMMENT '更新时间',
    `put_strategy` STRING COMMENT '投放策略 1:ad+ug 2:no ug app 3:no ad app',
    `put_apps` STRING COMMENT '投放的APP(新): appid1,appid2',
    `ad_categories` STRING COMMENT '广告类型(新) 1:大图 2:组图 3:原图',
    `star` BIGINT COMMENT '星级',
    `star2` BIGINT COMMENT '人工修正的星级',
    `audit_type` BIGINT COMMENT '0: 先投后审 1:先审后投',
    `mark` STRING COMMENT '备注',
    `source` STRING COMMENT '来源 union: 联盟业务 news:资讯合作 ',
    `pay_method` BIGINT COMMENT '结算方式 1:分成 2:CPM',
    `share_ratio` DOUBLE COMMENT '分成比例/结算值',
    `operation_cost` DOUBLE COMMENT '运营成本',
    `access_method` BIGINT COMMENT '接入方式 0: 未设置 1:JS 2:SDK 3:API',
    `adx_rit` STRING COMMENT '""',
    `stopped_at` BIGINT COMMENT '""',
    `reward_name` STRING COMMENT '激励名称',
    `reward_count` BIGINT COMMENT '激励数量',
    `reward_is_callback` BIGINT COMMENT '是否回调',
    `reward_callback_url` STRING COMMENT '激励回调url',
    `reward_security_key` STRING COMMENT '激励秘钥',
    `expect_imp_cnt` BIGINT COMMENT '预期展示量',
    `min_fill_rate` DOUBLE COMMENT '最低填充率',
    `splash_duration` DOUBLE COMMENT '开屏时间',
    `news_ad_count` BIGINT COMMENT '资讯合作-广告数量',
    `news_ad_strategy` STRING COMMENT '资讯合作-广告策略',
    `dsp_policy` BIGINT COMMENT '""',
    `orientation` BIGINT COMMENT '0：未知，1：垂直，2：水平',
    `mask_dsp_id` STRING COMMENT '屏蔽的dsp_id',
    `manual_score` DOUBLE COMMENT '""',
    `merge_score` DOUBLE COMMENT '""',
    `qgrade_coef` DOUBLE COMMENT '""',
    `qgrade_date` STRING COMMENT 'q3.0生成日期',
    `stable_score` DOUBLE COMMENT '算法Q值',
    `video_auto_play` BIGINT COMMENT '视频自动播放，1，wifi，2，wifi+4g，3，不自动播放',
    `video_voice_control` BIGINT COMMENT '视频声音控制，1，静音，2，有声音，默认1',
    `screenshot_url` STRING COMMENT '代码位截图url',
    `use_https` BIGINT COMMENT '是否使用https，0，否，1，是',
    `use_origin` BIGINT COMMENT '是否使用原图，0，否，1，是',
    `ov_preload` BIGINT COMMENT '原生(横版-竖版)视频预加载配置: 1表示wifi时提前下载, 2表示有网络即提前下载, 3表示播放时下载',
    `rv_preload` BIGINT COMMENT '激励视频网络预加载配置: 1表示wif时提前下载, 2表示有网络即提前下载',
    `rv_download_time` BIGINT COMMENT '激励视频发放时机: 0 - 100',
    `slide_banner` BIGINT COMMENT '1-非轮播，2-轮播',
    `mask_rule_id` BIGINT COMMENT '屏蔽规则id',
    `traffic_level` BIGINT COMMENT '流量分级: 1表示不出色情和恶心广告, 2表示不出低俗色情广告, 3表示不出恶心不适广告，4表示都出',
    `click_register` BIGINT COMMENT 'SDK点击注册方法设计：1.穿山甲默认点击注册方法；2.开发者接口配置注册方法',
    `support_skip` BIGINT COMMENT '全屏视频是否支持显示n秒后跳过：1-支持，2-不支持',
    `skip_duration` BIGINT COMMENT '跳过时长，默认前端设置为“5秒”',
    `origin_type` BIGINT COMMENT '1，模版渲染；2，自渲染',
    `style_type` BIGINT COMMENT '1，优选；2，自定义',
    `reward_skip` BIGINT COMMENT '激励视屏跳过时长，1-跳过，2-不跳过',
    `reward_duration` BIGINT COMMENT '激励视屏跳过时长:秒',
    `use_mediation` BIGINT COMMENT '是否使用穿山甲聚合',
    `ad_rollout_size` BIGINT COMMENT '广告铺开大小:全屏=1 插屏=2',
    `style_orientation` BIGINT COMMENT '代码位尺寸样式，1-竖版，2-横版，3-横/竖',
    `use_icon` BIGINT COMMENT '是否使用缩略标签:使用=1 不使用=2',
    `status_info` STRING COMMENT '状态三元组信息',
    `cpm_threshold_control` DECIMAL(10, 0) COMMENT '中国区固定分成门槛调控系数'
) PARTITIONED BY (`p_date` STRING COMMENT '日期分区') ROW FORMAT SERDE 'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe' WITH SERDEPROPERTIES ('serialization.format' = '1') STORED AS INPUTFORMAT 'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat' OUTPUTFORMAT 'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat' TBLPROPERTIES (
    'update_mode' = 'full',
    'transient_lastDdlTime' = '1660816999',
    'is_starred' = 'false',
    'is_core' = 'true',
    'ttl_extension' = '{"type":"mtime"}',
    'hide' = 'false',
    'status' = '3',
    'ttl' = '0',
    'ttlFirstExecTime' = '2022-08-18 17:30:20',
    'primary_key' = ''
)
